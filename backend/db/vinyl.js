// IMPORTING the client
const client = require('./client');

// vinyl FUNCTIONS

async function createVinyl({}) {
    try {
        
    } catch (error) {
        throw error
    }
}

async function getVinyl({}) {
    try {
        
    } catch (error) {
        throw error
    }
}

async function updateVinyl({}) {
    try {
        
    } catch (error) {
        throw error
    }
}

async function delteVinyl({}) {
    try {
        
    } catch (error) {
        throw error
    }
}

// EXPORTING the vinyl functions.

module.exports = {
    createVinyl,
    getVinyl,
    updateVinyl,
    delteVinyl,
}