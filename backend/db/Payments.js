// IMPORTING the client
const client = require('./client');

// creditCardInfo FUNCTIONS

async function creteCreditCardInfo({}) {
    try {
        
    } catch (error) {
        throw error
    }
}

async function getCreditCardInfo({}) {
    try {
        
    } catch (error) {
        throw error
    }
}

async function updateCreditCardInfo({}) {
    try {
        
    } catch (error) {
        throw error
    }
}

async function deleteCreditCardInfo({}) {
    try {
        
    } catch (error) {
        throw error
    }
}

// EXPORTING the creditCardInfo functions.

module.exports = {
    creteCreditCardInfo,
    getCreditCardInfo,
    updateCreditCardInfo,
    deleteCreditCardInfo,
}