// IMPORTING the client
const client = require('./client');

// vinyl_tags FUNCTIONS

async function createVinylTags({}) {
    try {
        
    } catch (error) {
        throw error
    }
}

async function getVinylTags({}) {
    try {
        
    } catch (error) {
        throw error
    }
}

async function updateVinylTags({}) {
    try {
        
    } catch (error) {
        throw error
    }
}

async function deleteVinylTags({}) {
    try {
        
    } catch (error) {
        throw error
    }
}

// EXPORTING the vinyl_tags functions.

module.exports = {
    createVinylTags,
    getVinylTags,
    updateVinylTags,
    deleteVinylTags,
}